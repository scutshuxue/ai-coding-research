# 整体架构对比：Cortex Memory vs PAI

> 对比维度：系统定位、模块组成、技术栈、集成方式、生态成熟度

---

## 1. 项目定位全景

```mermaid
graph TB
    subgraph "AI 应用栈"
        direction TB
        L1["用户层<br/>终端用户 / 开发者"]
        L2["应用层<br/>AI Agent / 聊天机器人 / 助手"]
        L3["平台层<br/>编排、记忆、技能、安全"]
        L4["基础设施层<br/>LLM API / 向量数据库 / 文件系统"]
    end

    CM["Cortex Memory<br/>记忆引擎"]
    PAI["PAI<br/>个人 AI 平台"]

    CM -.->|"专注于"| L3
    CM -.->|"连接"| L4
    PAI -.->|"覆盖"| L1
    PAI -.->|"覆盖"| L2
    PAI -.->|"覆盖"| L3

    style CM fill:#bbdefb,stroke:#1565c0
    style PAI fill:#f8bbd0,stroke:#c62828
    style L3 fill:#fff9c4
```

| 维度 | Cortex Memory | PAI |
|------|--------------|-----|
| 一句话定义 | AI 原生的长期记忆框架 | 个人 AI 基础设施平台 |
| 类比 | Redis/PostgreSQL（记忆专用） | 操作系统（AI 的 macOS） |
| 作者 | Sopaco（中国开发者/团队） | Daniel Miessler（安全专家） |
| 语言 | Rust | TypeScript + Bash |
| 版本 | v2.5.1 | v4.0.3 |
| 协议 | MIT | MIT |
| 运行方式 | 独立服务/库 | Claude Code 上层扩展 |
| 平台依赖 | 无（跨平台） | Claude Code（Anthropic 生态） |

---

## 2. 系统架构对比

### 2.1 Cortex Memory 架构

```mermaid
graph TD
    subgraph "用户接口层"
        CLI["cortex-mem-cli<br/>命令行工具"]
        Insights["cortex-mem-insights<br/>Svelte 5 Web Dashboard"]
    end

    subgraph "API & 集成层"
        Service["cortex-mem-service<br/>Axum REST API"]
        MCP["cortex-mem-mcp<br/>MCP Server"]
        Rig["cortex-mem-rig<br/>Rig 框架集成"]
    end

    subgraph "核心引擎"
        Core["cortex-mem-core<br/>业务逻辑"]
        Tools["cortex-mem-tools<br/>Agent 工具"]
        Config["cortex-mem-config<br/>配置管理"]
    end

    subgraph "外部依赖"
        QD[("Qdrant<br/>向量数据库")]
        LLM[("LLM Provider<br/>OpenAI/Azure/本地")]
        FS[("本地文件系统<br/>cortex:// VFS")]
    end

    CLI --> Core
    Insights -->|REST| Service
    Service --> Core
    MCP --> Tools
    Rig --> Tools
    Tools --> Core

    Core --> QD
    Core --> LLM
    Core --> FS

    style Core fill:#bbdefb,stroke:#1565c0,stroke-width:2px
```

**8 个 Crate 组成的模块化 Workspace：**

| Crate | 职责 | 代码量级 |
|-------|------|---------|
| cortex-mem-core | 核心引擎：VFS、搜索、提取、会话、层级、自动化 | ~65 个 .rs 文件 |
| cortex-mem-service | Axum REST API 服务器 | ~15 个 .rs 文件 |
| cortex-mem-cli | 命令行管理工具 | ~11 个 .rs 文件 |
| cortex-mem-tools | MCP 工具定义和操作封装 | ~10 个 .rs 文件 |
| cortex-mem-mcp | MCP Server（stdio 传输） | 2 个 .rs 文件 |
| cortex-mem-rig | Rig 框架集成 | 2 个 .rs 文件 |
| cortex-mem-config | TOML 配置管理 | 1 个 .rs 文件 |
| cortex-mem-insights | Svelte 5 前端 Dashboard | 前端项目 |

### 2.2 PAI 架构

```mermaid
graph TD
    subgraph "Claude Code 运行时"
        CC["Claude Code CLI"]
        CLAUDE["CLAUDE.md<br/>系统提示词"]
    end

    subgraph "PAI 核心系统"
        ALG["Algorithm 系统<br/>v3.7.0<br/>通用问题解决框架"]
        SKILL["Skill 系统<br/>11 个分类 / 63+ 技能"]
        HOOK["Hook 系统<br/>21 个生产级 Hook"]
        MEM["Memory 系统<br/>LEARNING/RELATIONSHIP/STATE"]
        SEC["Security 系统<br/>命令验证 + 注入防护"]
    end

    subgraph "辅助系统"
        VOICE["Voice 系统<br/>ElevenLabs TTS"]
        OBS["Observability<br/>Vue.js Dashboard"]
        NOTIFY["Notification 系统<br/>ntfy / Discord"]
        AGENT["Agent 系统<br/>14 个专用 Agent"]
    end

    subgraph "用户空间"
        TELOS["TELOS 目标体系<br/>10 个自我认知文件"]
        USER["USER/ 个人配置<br/>偏好/联系人/技术栈"]
    end

    CC --> CLAUDE
    CLAUDE --> ALG
    ALG --> SKILL
    ALG --> HOOK
    HOOK --> MEM
    HOOK --> VOICE
    HOOK --> NOTIFY
    SKILL --> AGENT
    USER --> CLAUDE
    TELOS --> CLAUDE
    SEC -->|验证| CC

    style ALG fill:#f8bbd0,stroke:#c62828,stroke-width:2px
    style CC fill:#e0e0e0,stroke:#616161,stroke-width:2px
```

**PAI v4.0.3 组件清单：**

| 系统 | 规模 | 核心文件 |
|------|------|---------|
| Algorithm | v3.7.0，PAI 的"引力中心" | PAI/Algorithm/v3.7.0.md |
| Skills | 11 个分类（Research、Security、Telos 等） | skills/ 目录 |
| Hooks | 21 个生产级 Hook | hooks/*.hook.ts |
| Agents | 14 个专用 Agent（Architect、Engineer 等） | agents/*.md |
| Tools | 45+ TypeScript 工具脚本 | PAI/Tools/*.ts |
| Memory | 4 个子目录 | MEMORY/ |
| Voice | ElevenLabs TTS 服务 | VoiceServer/ |
| Security | 命令注入 + Prompt 注入防护 | PAISECURITYSYSTEM/ |

---

## 3. 技术栈对比

```mermaid
graph LR
    subgraph "Cortex Memory 技术栈"
        R["Rust 1.86+"]
        Tokio["Tokio 异步运行时"]
        Axum["Axum Web 框架"]
        Qdrant_C["Qdrant 向量库"]
        Svelte["Svelte 5 (Dashboard)"]
        TOML["TOML 配置"]
    end

    subgraph "PAI 技术栈"
        TS["TypeScript"]
        Bash["Bash Shell"]
        Bun["Bun 运行时"]
        Vue["Vue.js (Observability)"]
        MD["Markdown 配置"]
        CC_T["Claude Code 原生"]
    end

    style R fill:#dcedc8
    style TS fill:#ffe0b2
```

| 维度 | Cortex Memory | PAI |
|------|--------------|-----|
| 核心语言 | Rust | TypeScript + Bash |
| 运行时 | Tokio（异步） | Bun |
| Web 框架 | Axum | 无（依赖 Claude Code） |
| 前端 | Svelte 5 | Vue.js |
| 向量数据库 | Qdrant（必需） | 无 |
| LLM 集成 | OpenAI 兼容 API + Rig 框架 | Claude Code 内置 |
| 协议支持 | REST API / MCP / CLI / Rust lib | Claude Code Hooks / Slash Commands |
| 配置格式 | TOML | Markdown + JSON + YAML |
| 包管理 | Cargo (workspace) | Bun + npm |

---

## 4. 集成方式对比

```mermaid
graph TB
    subgraph "Cortex Memory 集成路径"
        direction TB
        I1["Rust 库直接集成<br/>use cortex_mem_core"]
        I2["REST API<br/>任何语言 HTTP 调用"]
        I3["MCP Server<br/>Claude/Cursor/Copilot"]
        I4["CLI<br/>脚本/管道"]
        I5["Rig 框架<br/>Agent 工具注册"]
    end

    subgraph "PAI 集成路径"
        direction TB
        P1["Claude Code 原生<br/>CLAUDE.md + settings.json"]
        P2["Hook 系统<br/>事件驱动扩展"]
        P3["Skill 系统<br/>模块化能力"]
        P4["Slash Commands<br/>用户交互"]
    end

    style I1 fill:#e3f2fd
    style I2 fill:#e3f2fd
    style I3 fill:#e3f2fd
    style I4 fill:#e3f2fd
    style I5 fill:#e3f2fd
    style P1 fill:#fce4ec
    style P2 fill:#fce4ec
    style P3 fill:#fce4ec
    style P4 fill:#fce4ec
```

| 特性 | Cortex Memory | PAI |
|------|--------------|-----|
| 语言无关 | 是（REST API） | 否（TypeScript/Bash） |
| LLM 无关 | 是（OpenAI 兼容接口） | 否（Claude Code 专属） |
| 可嵌入性 | 高（Rust crate / REST / MCP） | 低（绑定 Claude Code） |
| 多租户 | 原生支持 | 单用户设计 |
| 独立部署 | 是（独立二进制） | 否（需要 Claude Code） |

---

## 5. 功能范围对比

```mermaid
graph TB
    subgraph "功能维恩图"
        direction TB

        subgraph "仅 Cortex Memory"
            C1["向量语义搜索"]
            C2["三层记忆层级 L0/L1/L2"]
            C3["多租户隔离"]
            C4["Embedding 缓存"]
            C5["Benchmark 评估框架"]
            C6["cortex:// 虚拟文件系统"]
        end

        subgraph "共同领域"
            S1["长期记忆存储"]
            S2["会话管理"]
            S3["MCP 协议支持"]
            S4["Web Dashboard"]
            S5["CLI 工具"]
        end

        subgraph "仅 PAI"
            P1["Algorithm 自进化系统"]
            P2["63+ Skill 模块"]
            P3["21 Hook 事件驱动"]
            P4["TELOS 目标体系"]
            P5["Voice/TTS 语音"]
            P6["Security 命令防护"]
            P7["14 专用 Agent"]
            P8["情感分析 + 评分捕获"]
        end
    end

    style C1 fill:#bbdefb
    style C2 fill:#bbdefb
    style C3 fill:#bbdefb
    style C4 fill:#bbdefb
    style C5 fill:#bbdefb
    style C6 fill:#bbdefb
    style S1 fill:#e8eaf6
    style S2 fill:#e8eaf6
    style S3 fill:#e8eaf6
    style S4 fill:#e8eaf6
    style S5 fill:#e8eaf6
    style P1 fill:#f8bbd0
    style P2 fill:#f8bbd0
    style P3 fill:#f8bbd0
    style P4 fill:#f8bbd0
    style P5 fill:#f8bbd0
    style P6 fill:#f8bbd0
    style P7 fill:#f8bbd0
    style P8 fill:#f8bbd0
```

---

## 6. 生态与成熟度

| 维度 | Cortex Memory | PAI |
|------|--------------|-----|
| 开发阶段 | 功能完善，有 benchmark 验证 | 快速迭代，频繁 breaking changes |
| 发版节奏 | 稳定（v2.5.1） | 激进（2 个月内从 v2.3 到 v4.0.3） |
| 文档质量 | 优秀（中英双语，API 文档完整） | 良好（概念文档丰富，技术细节分散） |
| 测试覆盖 | 有单元测试 + benchmark 评估框架 | 以手动验证为主 |
| 社区 | 较小（GitHub Star 较新） | 活跃（Daniel Miessler 社区，30+ 社区贡献） |
| 平台支持 | 跨平台（Rust 编译） | macOS + Linux（无 Windows） |
| 示例应用 | Cortex TARS（TUI 聊天应用） | 内置于系统本身 |

---

## 7. 运行时依赖对比

```mermaid
graph LR
    subgraph "Cortex Memory 运行需要"
        CR1["Rust 1.86+"]
        CR2["Qdrant 1.7+"]
        CR3["OpenAI 兼容 API"]
        CR4["Embedding API"]
    end

    subgraph "PAI 运行需要"
        PR1["Claude Code<br/>(Anthropic 订阅)"]
        PR2["Bun 运行时"]
        PR3["Git"]
        PR4["ElevenLabs API<br/>(可选，语音)"]
    end

    style CR2 fill:#ffccbc
    style PR1 fill:#ffccbc
```

**关键差异**：Cortex Memory 需要自建 Qdrant 向量数据库（运维成本），PAI 需要 Claude Code 订阅（费用成本）。

---

## 8. 总结：引擎 vs 平台

```mermaid
graph TB
    Engine["记忆引擎<br/>(Cortex Memory)"]
    Platform["AI 平台<br/>(PAI)"]

    Engine -->|"做一件事做到极致"| E1["精确的语义记忆检索"]
    Engine -->|"可被任何应用集成"| E2["语言无关、平台无关"]
    Engine -->|"工程化品质"| E3["Rust 性能 + Benchmark 验证"]

    Platform -->|"完整的用户体验"| P1["从目标设定到任务执行"]
    Platform -->|"自我进化能力"| P2["Algorithm 驱动的持续学习"]
    Platform -->|"深度绑定生态"| P3["Claude Code 原生集成"]

    style Engine fill:#bbdefb,stroke:#1565c0,stroke-width:2px
    style Platform fill:#f8bbd0,stroke:#c62828,stroke-width:2px
```

**Cortex Memory** 是一个遵循 UNIX 哲学的**专用工具**——做好"记忆"这一件事，通过标准接口让任何系统都能使用。

**PAI** 是一个以人为中心的**完整平台**——它不仅记忆，还理解目标、编排技能、捕获情感、驱动进化。但它深度绑定在 Claude Code 生态中。

两者的架构差异本质上反映了两种不同的产品思路：**基础设施 vs 应用平台**。
